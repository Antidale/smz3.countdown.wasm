@inherits FlagsetBase

@code {
    //list of boss objectives (odin, golbex, 3 extra)
    //list of 5 pt objs (forge, pink, odin spot, 2 extra)
    //list of 7 pt objs (falcon, mura, 2 extra)
    //giant

    //method for calculating the total dark matter count based on objective completion
    //display dark matter count

    //flag notes
    //kmain/char free KI
    //twildish t7 playable (pro miabs)
    //scabins/free
    //bchaos no gauntlet
    //choasburn, whichbez
    //enoexp (probalby not needed to recall?)
    //vanilla miab
}